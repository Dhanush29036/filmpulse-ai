# ─────────────────────────────────────────────────────────────────────────────
# FilmPulse AI — Environment Variables Template
# Copy to .env and fill in real values before running docker compose
# NEVER commit .env to git (it's in .gitignore)
# ─────────────────────────────────────────────────────────────────────────────

# ── Security ─────────────────────────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=filmpulse-super-secret-key-change-in-production

# ── PostgreSQL (AWS RDS in production) ───────────────────────────────────────
POSTGRES_USER=filmpulse
POSTGRES_PASSWORD=filmpulse_secret_change_me
POSTGRES_DB=filmpulse
# Override for AWS RDS:
# DATABASE_URL=postgresql://filmpulse:password@your-rds-endpoint.rds.amazonaws.com:5432/filmpulse

# ── MongoDB (Mongo Atlas in production) ──────────────────────────────────────
MONGO_USER=filmpulse
MONGO_PASSWORD=filmpulse_mongo_change_me
MONGO_DB=filmpulse
# Override for Mongo Atlas:
# MONGO_URL=mongodb+srv://filmpulse:password@cluster0.xxxxx.mongodb.net/filmpulse?retryWrites=true&w=majority

# ── Redis ────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=filmpulse_redis_change_me

# ── Phase 6: Real-Time API Keys (optional — graceful fallback if missing) ────
# Twitter/X Developer Portal — https://developer.twitter.com
# Create app → "Read" permissions → Bearer Token
TWITTER_BEARER_TOKEN=

# Google Cloud Console → YouTube Data API v3 → API Key
# https://console.cloud.google.com/apis/library/youtube.googleapis.com
YOUTUBE_API_KEY=

# Google Trends: no API key needed (pytrends uses unofficial endpoint)
# Note: pytrends may be rate-limited; fallback to synthetic data is automatic

# ── Frontend ─────────────────────────────────────────────────────────────────
# Vercel/production URL of your backend
VITE_API_URL=http://localhost:8000/api/v1

# ── AWS (for CI/CD deployment) ───────────────────────────────────────────────
AWS_REGION=ap-south-1
# ECR_REGISTRY=123456789012.dkr.ecr.ap-south-1.amazonaws.com
# EC2_HOST=ec2-13-234-xxx-xxx.ap-south-1.compute.amazonaws.com
# EC2_USER=ubuntu

# ── HuggingFace (optional — prevents re-download each cold start) ─────────────
# Pre-download BERT: python -c "from transformers import pipeline; pipeline('sentiment-analysis')"
# TRANSFORMERS_OFFLINE=1  # uncomment after first download

# ─────────────────────────────────────────────────────────────────────────────
# DEPLOYMENT CHECKLIST:
# [ ] Rotate SECRET_KEY before going live
# [ ] Use AWS RDS for PostgreSQL (not docker postgres in production)
# [ ] Use Mongo Atlas free tier (not docker mongo in production)
# [ ] Add TWITTER_BEARER_TOKEN for live sentiment data
# [ ] Add YOUTUBE_API_KEY for live YouTube data
# [ ] Point VITE_API_URL to your EC2 IP / domain
# [ ] Configure SSL via Let's Encrypt (certbot) on EC2
# ─────────────────────────────────────────────────────────────────────────────
